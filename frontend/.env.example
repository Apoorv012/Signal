# Where the backend API lives. Optional in development: when unset, the app uses
# http://<the host the page was opened from>:8000 (so opening it from a phone on the LAN works).
# Required in production, e.g. https://signal-api.example.com (the WebSocket URL is derived from it).
NEXT_PUBLIC_API_URL=http://localhost:8000
